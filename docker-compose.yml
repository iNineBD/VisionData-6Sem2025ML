services:
  app:
    build: .
    container_name: predict_tickets_app
    ports:
      - "8000:8000"

  mlflow:
    image: ghcr.io/mlflow/mlflow:v3.5.0
    container_name: mlflow_server
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    command: mlflow server --host 0.0.0.0 --port 5000 --backend-store-uri /mlflow/mlruns --default-artifact-root /mlflow/mlruns --allowed-hosts '*'
    ports:
      - "5000:5000"
    volumes:
      - ./mlruns:/mlflow/mlruns
    restart: unless-stopped
